import styled from 'styled-components'

export const SectionFormInscricao = styled.section`
    display: flex;
    flex-direction: column;
    width: 100vw;
    background-color: var(--white-fixed);
`