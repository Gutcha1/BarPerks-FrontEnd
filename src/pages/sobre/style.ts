import styled from 'styled-components'

export const Main = styled.main`
    display: flex;
    width: 100vw;
    height: fit-content;
    background-color: var(--white-fixed);
    padding-top: 80px;
`